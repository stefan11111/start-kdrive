/* increase if this causes problems */
#define SIZE_MAX 512

/* add devices that cause problems */
/* for example, devices that aren't keyboards, but advertise themselves as such */
static char* ignore_keyoards[] = {"Razer Razer Basilisk V2"};
static char* ignore_mice[] = {NULL};
